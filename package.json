{
  "name": "calendar-todo-app",
  "type": "module",
  "version": "0.0.3",
  "private": true,
  "description": "Calendar Todo App - Monorepo",
  "author": "",
  "license": "MIT",
  "keywords": [
    "todo",
    "calendar",
    "monorepo"
  ],
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:client": "pnpm --filter @todo-app/client dev",
    "dev:server": "node scripts/run-cargo.mjs run --manifest-path src-tauri/Cargo.toml --no-default-features --example gtd-server",
    "build": "pnpm build:client && pnpm build:server",
    "build:client": "pnpm --filter @todo-app/client build",
    "build:server": "node scripts/run-cargo.mjs build --manifest-path src-tauri/Cargo.toml --no-default-features --example gtd-server",
    "start:server": "node scripts/run-cargo.mjs run --release --manifest-path src-tauri/Cargo.toml --no-default-features --example gtd-server",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "clean": "pnpm -r exec rm -rf node_modules dist .tanstack && rm -rf node_modules",
    "install:all": "pnpm install",
    "tauri": "node scripts/run-tauri.mjs",
    "tauri:prepare": "node scripts/prepare-tauri-resources.mjs",
    "tauri:dev": "node scripts/run-tauri.mjs dev",
    "tauri:build": "pnpm tauri:prepare && node scripts/run-tauri.mjs build"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@eslint-react/eslint-plugin": "catalog:",
    "@tauri-apps/cli": "catalog:",
    "eslint": "catalog:",
    "eslint-plugin-format": "catalog:",
    "eslint-plugin-react-hooks": "catalog:",
    "eslint-plugin-react-refresh": "catalog:",
    "prettier": "catalog:"
  }
}
